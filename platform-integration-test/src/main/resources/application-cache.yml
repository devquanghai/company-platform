# New
platform:
  cache:
    enabled: true
    provider: redis
spring:
  cache:
    redis:
      use-key-prefix: true
      key-prefix: ${spring.application.name}
      customize:
        cache:
          ttl: 3600

  data:
    redis:
      host: localhost
      port: 6379
